| Type      | Directory            |
|-----------|----------------------|
| Service   | ~/Library/Services   |

Open with Tor Browser (Service)
* Usage: Select something and right click, "Services > Open with Tor Browser"
* If a link is selected, opens the link in Tor
* If the selected text contains a URL, opens the URL in Tor
* Otherwise, search selection with DuckDuckGo in Tor
